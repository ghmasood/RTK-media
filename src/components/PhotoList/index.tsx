import React from "react";
import { IAlbumData } from "../../types";

interface IPhotosListProp {
  album: IAlbumData;
}
function PhotosList({ album }: IPhotosListProp) {
  return <div>fnsadfm</div>;
}

export default PhotosList;
